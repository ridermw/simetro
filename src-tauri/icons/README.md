# Tauri icons placeholder.
#
# Real icons (icon.png, icon.icns, icon.ico, plus platform-specific
# sizes) ship with the first signed build. For now, `cargo tauri
# icon` can generate the full set from a single 1024x1024 source.
