[package]
name = "xtask"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Project-specific build helpers — not shipped to users."
publish = false

[lints]
workspace = true

[[bin]]
name = "xtask"
path = "src/main.rs"

[dependencies]
# Intentionally minimal — std-only. The xtask runs human-driven
# smokes; speed-of-add matters more than fancy CLI plumbing.
